#include "main.h"
/**
 * _islower - Entry point
 *
 *
 * Description: Size of
 *
 * Return: On success 0
 */
int _islower(int c)
{
if (c >= 'A' && c <= 'Z')
return (0);
else if (c >= 'a' && c <= 'z')
return (1);
}
